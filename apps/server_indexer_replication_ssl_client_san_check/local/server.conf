{% for port in conf.replication_ports_ssl %}
[replication_port-ssl://{{port.port}}]
requireClientCert            = {{ port.require_client_cert|string|lower|default("false",true)}}
{%- if conf.peers is defined %}
sslAltNameToCheck = {{conf.peers|join(",")}}
{% endif %}